# stepintocrypto
step into crypto

## Guestbook DB (Supabase)

1. Supabase SQL editor에서 `supabase/guestbook.sql` 실행
2. `guestbook-config.js`에 아래 값 입력
   - `window.SIC_SUPABASE_URL`
   - `window.SIC_SUPABASE_ANON_KEY`
3. `guestbook.html` 접속 후 목록 조회/등록 동작 확인
